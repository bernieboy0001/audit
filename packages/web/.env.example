# Where the agent API runs (set this to your deployed worker, e.g. Render)
NEXT_PUBLIC_AGENT_URL=http://localhost:8787

# Explorer used for on-chain proof links
NEXT_PUBLIC_EXPLORER_URL=https://sepolia.basescan.org